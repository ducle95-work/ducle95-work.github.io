{
    "name": "ducle-github-io",
    "version": "1.0.0",
    "description": "Duc Le personal portfolio site",
    "scripts": {
        "manifest": "node scripts/generate-manifest.js",
        "dev": "npm run manifest && http-server -p 8080 -c-1 --cors"
    },
    "devDependencies": {
        "http-server": "^14.1.1"
    }
}